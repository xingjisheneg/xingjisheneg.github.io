---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArticleCard from '../../components/ArticleCard.astro';
import { getCollection } from 'astro:content';
import { listPages, site } from '../../config';

const essays = (await getCollection('essays')).sort((a,b) => b.data.date.localeCompare(a.data.date));
const cfg = listPages.essays;
---
<BaseLayout title={`${cfg.pageTitle} · ${site.name}`} active="essays">
  <div class="container">
    <div class="hero" style="padding-top:0">
      <div class="vol">{cfg.vol}</div>
      <h1 style="font-size:clamp(2rem,5vw,2.8rem)">{cfg.title}</h1>
      <p class="lede">{cfg.lede}</p>
    </div>
    <div class="cards">
      {essays.map(it => <ArticleCard item={it} section="essays" />)}
    </div>
  </div>
</BaseLayout>
