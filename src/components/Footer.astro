---
import { footer } from '../config';
---
<footer class="site-foot">
  {footer.parts.map((p, i) => (
    <>
      {i > 0 && <span class="v">·</span>}
      <span>{p}</span>
    </>
  ))}
  {footer.rssLabel && <><span class="v">·</span><a href="/rss.xml">{footer.rssLabel}</a></>}
</footer>
